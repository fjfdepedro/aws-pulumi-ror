
# Ruby on Rails Server Using Amazon EC2 with Pulumi


    ```bash
    $ pulumi up
    ```


    ```bash
    $ pulumi stack output
    ```

    ```bash
    $ curl $(pulumi stack output websiteURL)
    ```
    
    ```bash
    $ pulumi destroy --yes
    $ pulumi stack rm --yes
    ```
