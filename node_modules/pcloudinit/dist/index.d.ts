export declare type Init = {
    [config: string]: InitConfig;
};
export interface InitConfig {
    files?: {
        [path: string]: InitFile;
    };
    packages?: {
        [manager: string]: InitPackages;
    };
    commands?: {
        [command: string]: InitCommand;
    };
    services?: {
        [manager: string]: InitServices;
    };
}
export interface InitFile {
    content: Promise<string>;
    mode: string;
    owner: string;
    group: string;
}
export declare type InitPackages = string[];
export interface InitCommand {
    command: string;
    test?: string;
}
export declare type InitServices = {
    [service: string]: InitService;
};
export interface InitService {
    enabled?: boolean;
    ensureRunning?: boolean;
}
export declare function createUserData(configs: string[], init: Init): Promise<string>;
export declare function renderConfigFile(path: string, config: any): Promise<string>;
